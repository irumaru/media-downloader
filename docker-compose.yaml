services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    working_dir: /app
    volumes:
      - ./backend:/app
      - ./config.yaml:/app/config.yaml
      - ./data:/app/data
      - go-cache:/root/.cache/go-build
      - go-mod:/root/go/pkg/mod
    ports:
      - "8080:8080"
    command: air

volumes:
  go-cache:
  go-mod:

[build]
  cmd = "go build -o ./tmp/main ."
  bin = "./tmp/main"
  delay = 1000
  exclude_dir = ["tmp"]
  include_ext = ["go", "yaml"]
  stop_on_error = true
  kill_delay = "1s"

[log]
  time = true

[misc]
  clean_on_exit = true
